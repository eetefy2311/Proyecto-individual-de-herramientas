library(ggplot2)
library(tidyverse)

plt <- iris %>% 
  ggplot(aes(x=Sepal.Length, y=Sepal.Width, color = Species)) + 
  geom_point() +
  theme_minimal()

ggsave("figure/iris_plot.pdf", plot = plt, width = 6, height = 4)

